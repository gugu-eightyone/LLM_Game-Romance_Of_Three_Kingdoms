"""게임 밸런스·캘리브레이션 상수 — 로직과 분리한 튜닝 손잡이 한 곳(플레이테스트로 조정).

로직(models·engine)을 import하지 않는다(순환 방지). 여긴 순수 상수만.
설계 근거·트레이드오프는 docs/DISCUSSION.md 참조.
"""

# --- LLM 호출 ---
MODEL = "gpt-5.4-mini"           # 각국 결정·judge 공통(Q5). 6종 비교로 확정(2026-08-28, docs/MODEL_COMPARISON.md): 품질·속도 동시 1위.
TEMPERATURE = 0.3                # 규칙 준수·숫자 대조가 주 임무라 저온. ⚠구세대 gpt-5(5-mini/5-nano)는 temp=1 고정 — 0.3을 보내면 전 호출 400→조용한 폴백(게임 식물화). 모델 바꿀 땐 확인.

# --- 전략 서술(LLM) ---
STRATEGY_MAX_CHARS = 50          # 전략 자유서술 상한. 담화 아닌 지시라 짧게. [[DISCUSSION#9-8]]
STRATEGY_MODIFIER_BOUND = 0.30   # 전투 전략 심판 채점 → 전투력 보정 상한(±). ⭐2026-09-02 배선 완료 — 비대칭 로그(10점 척도):
                                 # mod = BOUND×ln(점수/5)/ln(2) → 10점=+0.30 도달 가능(⭐"찍기 어렵지 않게"), 5점=0,
                                 # 1~2점=−0.30 급강하(터무니없는 전략=무전략보다 손해, §9-12 원목적).
DOMESTIC_MODIFIER_BOUND = 0.30   # ⭐모병 대사 심판 보정 상한(사용자 "전투 판정처럼 ±30%"). 같은 곡선 재사용.

# --- 이동/지형 ---
FACTION_SPEED = {"오": 1.25}     # 진군 진행도/월(세력별). 미지정=DEFAULT_SPEED. 오=장강 고속. [[DISCUSSION#9-9]]
DEFAULT_SPEED = 1.0              # 기본 진군 속도. 이동 threshold=거리(개월).
RIVER_CROSS_PENALTY = 1          # 강 구간 도하 지연(개월). 위·촉만(오는 수전 면제).
FACTION_NAVAL = {"오": 2, "위": 1, "촉": 1}  # 증분2 야전 수전 보정용 예약(이동 미사용).

# --- prep(조기도착 선취) ---
PREP_RATE = 0.2                  # 조기도착 잉여(개월) → 첫 교전 우세도 보정 환산.
PREP_CAP = 0.10                  # prep 보정 상한(결정론 코어 지배 유지, MODIFIER_BOUND보다 작게).

# --- 전투 ---
GENERAL_SCALE = 500              # 통솔 보정 스케일: 병력배수 = 1 + 최고통솔/SCALE
WALL_DEFENSE = 2500              # 성벽 레벨당 수비 병력환산 보너스(수성 우위 손잡이).
WALL_MANNING = 1000              # 성벽 1레벨 정원(병력). 보너스 = min(wall, 병력/정원)×WALL_DEFENSE — "지킬 사람 있는 만큼만"(빈 성벽 혼자 못 싸움). 현 주둔 스케일(18k+)선 정상 케이스 무영향, 출성 배분 리스크의 이빨. [[DISCUSSION#9-20]]
SORTIE_SLOW_CAP = 0.75           # 출성의 공성 감속 상한. 감속 = min(CAP, 출성병력/공성병력) — 규모 비례, 정지는 없음(§9-20 "감속이지 정지 아님").
ATTRITION_RATE = 0.10            # 교전 월 손실 계수(상대 전투력 대비).
SIEGE_RATE = 3                   # 교전 우세도(전력비-1) → 성벽 피해 계수(HP화 전엔 진행도/월).
SIEGE_BASE = 2                   # 성벽 최대 HP의 기본 몫: 최대 = (SIEGE_BASE + 성벽레벨) × WALL_HP_SCALE.
# --- 성벽 HP(⭐2026-09-01 A안 확정 — 아래 수치 3개는 ⚠미확정 가안(경제 수치와 동급): 플레이 감각·브리핑 보고 확정 ---
WALL_HP_SCALE = 1000             # 구 진행도 1pt = HP 1000 등가 환산 → 현행 함락 소요 턴 보존 지점에서 출발.
WALL_REPAIR_GOLD_PER_HP = 3      # 성벽보수: 금 3 → HP 1 (구 "금 3000→1레벨" 등가). 파손분만큼만 과금(초과 지출 안 받음).
WALL_REGEN_PER_TURN = 200        # 평시(피침 아닐 때) 월 자연회복. 공성·접근 중=불가 — 전간기 복구용, 공성 버티기용 아님.
WALL_CAPTURE_HEAL_RATIO = 0.2    # 점령 턴 말 응급 보수: 주인 바뀐 성 = 최대 HP의 이 비율만큼 회복(⭐사용자 1/5).
                                 # 방금 깬 성이 무방비(HP 0)로 다음 턴을 맞지 않게 — 우세도 게이트와 한 쌍.

# --- 내정 ---
DOMESTIC_GAIN = 2                # 금 1 지출 → 식량/병력 2 (식량증산=선형 유지, 모병=아래 로그 체감의 초기 기울기).
RECRUIT_CURVE_SCALE = 5000       # ⚠미확정 가안(⭐모병 로그 체감, concave down): 병력 = GAIN×SCALE×ln(1+금/SCALE).
                                 # 금 1000→1812(91%)·5000→6931(69%)·20000→16094(40%) — 금 도배 스노볼 카운터.
ALLIANCE_DEFAULT_MONTHS = 12     # ⭐동맹 기한 기본값(개월). months=0 제안은 이 값. 만료=명예 종료(파기와 구분).

# --- 경제(매턴 수입·병량 소모, ⭐사용자 2026-08-30 "금은 언제 올라?") ---
CITY_INCOME_GOLD = 300           # 도시 규모(size) 1당 월 금 수입. 위 16규모=4800/월 > 오 2700 > 촉 2400(국력 반영).
CITY_INCOME_FOOD = 500           # 규모 1당 월 식량 수입. 규모2+병2만 도시 ≈ 수지 균형.
TROOPS_PER_FOOD = 20             # 출전(원정) 병사 20명 = 월 식량 1 소비. 대군 유지비 = 모병 스노볼 카운터.
GARRISON_TROOPS_PER_FOOD = 40    # ⭐차등(사용자 2026-09-02): 주둔 병사는 원정의 절반만 소모(40명당 1) — 전쟁이 비싸다.
STRANDED_DESERTION = 0.3         # ⚠미확정 가안: 굶주림 탈영률 — 못 먹인 인원×이 비율이 매턴 탈영(주둔·원정·고립 공용.
                                 # ⭐2026-09-02 통일: 부족분 전액 즉발 절벽 폐기 → 말라죽는 곡선+대응 창. 2턴 후 49% 잔존).
FOOD_ALERT_MONTHS = 3            # 군량 경보: 현 소모율로 이 개월 내 고갈이면 brief·UI에 결정론 경고(⭐사용자 "3달 내 위험 시그널").

# --- 호송/명령 상한 (2026-08-27) ---
ESCORT_MIN_TROOPS = 200          # 병사·물자·포로 호송 최소 호위 병력. 장수 단독 이동은 면제.
MAX_ORDERS_PER_TURN = 4          # 세력당 턴 명령 상한(LLM 폭주 안전핀). 초과분 잘라내고 [환각] 로깅(A층 표면).

# --- 포로화/함락 ---
CAPTURE_FLOOR = 0.2              # 포획확률 최솟값(가산 아님). 포획확률 = max(FLOOR, 포위도²). [[DISCUSSION#9-10]]
SIEGE_RETREAT_SURVIVAL = 0.5     # 성벽 돌파 함락 시 잔존 수비병 생존율(패주 손실 절반). 인접 아군 도시로 퇴각, 완전 포위=퇴로 없음→전멸. 청야/함정 반격은 judge 몫(defer).

# --- 사기(전투력 배수) ---
MORALE_COMBAT_BAND = 0.3        # 사기 전투력 배수 폭: 1 + (morale−50)/50 × BAND (m0→0.7·m50→1.0·m100→1.3). 통솔의 ~2배, 스노볼 억제. [[DISCUSSION#9-10]]

# --- 사기 이벤트 훅(§9-10 배선, ⭐2026-08-30 사용자 정의: 사기=세력의 "정신적 무장" ---
# 전역 공통값이므로 국지 전투(공성 격퇴 등) 판정은 제외, 세력 단위 굵직한 사건만 크게.
MORALE_CITY_TAKEN = 12          # 함락 성공 → 승자 전역 사기 +
MORALE_CITY_LOST = -15          # 도시 상실 → 패자 −
MORALE_RULER_CAPTURED = -30     # 군주 피랍 → 그 세력 −(최대 사건) — 포획의 기계적 디메리트(§9-21 몸값·처형 가치의 근원)
MORALE_FEAST_CAP = 80           # 사기진작(만찬) 천장(⭐사용자 지정 80) — 그 위는 승리(함락)로만.

# --- 항복 권유(외교, §9-22 확장) ---
SURRENDER_CITY_GATE = 2         # 성립 게이트(⭐2026-09-01 교체): 상대 잔여 도시 ≤ 이 값 + 제안측 국력 우위.
                                # 구 "국력 3배" 산식은 대세력 흡수 직후 차순위에 바로 성립하는 민감도 문제로 폐기.
SURRENDER_CITY_WEIGHT = 5000    # 국력 = 총병력(도시+출전) + 도시수 × 이 가중치.

# --- 담화(포로 처분: 즉결 질의·설득) [[DISCUSSION#9-21]] ---
PERSUADE_BASE = 0.2              # 설득 기본 확률(LLM 경로). P = (BASE + 설득장수 지력/INTEL_SCALE) × (1 − 포로 충의/100).
PERSUADE_INTEL_SCALE = 300       # 지력 → 확률 환산(지력 90 = +0.3). 지력의 첫 실전 배선.
PARLEY_MAX_ROUNDS = 3            # 플레이어 담화 라운드 상한(비용 상한 = 헤드라인 제약).
# 설득 = 행동 턴의 명령(Persuade Action)이라 명령 슬롯을 자연 소모(⭐사용자 재설계 — 별도 차감 장치 불필요).
# 플레이어 경로엔 숫자 상한 없음(⭐cap 폐지): 충의는 페르소나 연기+심판 채점에 내재, P=(점수−1)/9 직통(10점 척도).

# --- 야전(증분2: 지속 전투·요격·구원군·마일스톤) ---
UNIT_MORALE_COMBAT_DROP = 3     # 교전 1회당 부대 사기 감소(전투 소모). 보급차단 대량감소·재연결 회복은 추후.
# 협공 보너스는 결정론 상수로 두지 않음 — "좋은 협공인가"는 배치 따라 뒤집히는 판단이라 judge 넛지 몫.
# 협공 효과 자체는 "두 번 맞는다"(수성 라운드+구원군 라운드)로 공짜 발생. [[DISCUSSION#9-10]]
ROUT_MORALE_THRESHOLD = 30      # 이 사기 미만이면 확률적 강제 퇴각 발동. 확률=((T−m)/T)²(아래로 볼록, m=0→1). seeded RNG.
FIELD_RETREAT_LOSS = 0.2        # 강제 퇴각 시 추가 병력 손실(급히 빠지는 대가). judge가 경감 가능(나중).
FIELD_CAPTURE_BASE = 0.15       # 야전 전멸 시 장수 포획 기본확률 × 그 턴 교전 적 부대 수. 최근접 적 도시 호송(탈영 없음). [[DISCUSSION#9-10]]
